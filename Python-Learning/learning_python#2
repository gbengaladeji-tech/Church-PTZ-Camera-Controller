camera = 1
pan_speed = 5
tilt_speed = 5
# Normalize the habit of using Lowercase letters when coding
numbers = [10, 20, 30]
print(numbers[0])
print(numbers[1])
print(numbers[2])
# In python when printing numbers, you can print in a certain order you want by using the above code, starting from 0 to the length of the list.
